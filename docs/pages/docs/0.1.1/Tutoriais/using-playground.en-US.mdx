---
title: 'Usando playground'
description: 'Este guia tem como finalidade te mostrar como usar o componente Playground.'
---

import { FaAccessibleIcon } from 'react-icons/fa';

import { Playground } from '@engrafia/ui';

import { Button } from '@nextui-org/react';

# Usando Playground

Playground é um componente que permite que outros componentes sejam renderizados dentro de um ambiente isolado,
além de renderizar o Playground também permite editar o código e ver o resultado da edição ao vivo. Este recurso é muito útil em casos de documentação de componentes.

## Exemplo

<Playground>
  <Button>
    <FaAccessibleIcon />
  </Button>
</Playground>

## Código

```mdx
---
title: 'Usando playground'
description: 'Este guia tem como finalidade te mostrar como usar o componente Playground.'
---

import { FaAccessibleIcon } from 'react-icons/fa';

import { Playground } from '@engrafia/ui';

import { Button } from '@nextui-org/react';

# Usando Playground

Playground é um componente que permite que outros componentes sejam renderizados dentro de um ambiente isolado,
alem de renderizar o Playground também permite copiar, e editar o código e ver o resultado da ediçao ao vivo. Este recurso é muito útil em casos de documentação de componentes.

## Exemplo

<Playground>
  <Button>
    <FaAccessibleIcon />
  </Button>
</Playground>
```

O componente `Playground` deve ser importado do pacote `@engrafia/ui`, como se vê no exemplo acima.

<Playground>
  <a>Link</a>
</Playground>
