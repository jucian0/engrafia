import { styled } from '@nextui-org/react';

export const Wrapper = styled('div', {
  display: 'flex',
  justifyContent: 'space-between',
  margin: '1.125rem 0',

  a: {
    textDecoration: 'none',
    display: 'flex',
    alignItems: 'center',
  },
});
